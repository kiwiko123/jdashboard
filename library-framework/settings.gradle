pluginManagement {
    repositories {
        mavenCentral()
    }
    includeBuild('../build-conventions')
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

includeBuild '../platforms'

rootProject.name = 'library-framework'
include 'data'
