dependencyResolutionManagement {
    repositories {
        gradlePluginPortal()
    }
}

includeBuild '../platforms'

rootProject.name = 'build-conventions'
include 'core-commons'
include 'spring-boot-web-service'